events {
    worker_connections 1024;
}

http {
    server {
        listen 80;
        
        location / {
            proxy_pass http://host.docker.internal:5173; # Frontend
        }
        
        location /api/ {
            proxy_pass http://host.docker.internal:3000/; # Backend
        }
    }
}
